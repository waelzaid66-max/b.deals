# 🚀 BANCO V23: THE MISSING OPERATIONAL LAYERS

**MISSION:** Close all operational gaps in the Lead pipeline, Admin control, and Lifecycle management.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 1. LEAD VALIDATION ENGINE (CPL PROTECTION)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
To ensure no fake leads are billed, implement `LeadValidatorService`:
* **Validation Logic:** * Limit 1 WhatsApp/Call click per user per listing per hour.
    * IP-Fingerprinting: Block actions from suspicious proxies/VPNs.
    * Time-based gating: A lead is only valid if the `detail_view` duration > 5 seconds.
* **Database Action:** If valid, proceed to `BillingEngine` deduction. If invalid, log as `spam_attempt` for Admin review.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 2. THE ADMIN CONTROL ROOM (FULL OPERATIONS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Must include these specific views for the Owner (You):
* **Revenue Heatmap:** Which listing categories and which dealers are generating the most CPL revenue?
* **Moderation Queue:** Any listing with "High Value" (Cars > 2M EGP) or "Industrial" MUST be reviewed by you before it hits the `active` feed.
* **Global Kill Switch:** A UI toggle in Admin Dashboard to "Pause All Ad Injections" or "Switch Maintenance Mode" across all mobile/web platforms instantly.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 3. LISTING LIFECYCLE MANAGEMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
* **Status Transitions:**
    * `Draft` -> `Pending Approval` (Moderation) -> `Active` (Live) -> `Sold/Archived`.
* **Auto-Archive Policy:** The Cron Job must perform a "Warning Phase":
    * Day 85: Send notification "Your listing will expire in 5 days".
    * Day 90: Auto-archive.
* **Re-Activation:** Users can re-activate archived listings only if they pay the `Boost` fee or have remaining subscription slots.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 4. SUPPORT & COMPLAINT SYSTEM (INTEGRATED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
* **In-App Support:** Every listing has a "Report this listing" button.
* **Admin Action:** When a report is submitted, the Admin Dashboard shows the report, the user profile, and the listing link. Admin can choose: `Delete`, `Warning`, or `Dismiss`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## EXECUTION DIRECTIVE:
"Integrate these specific logic gates into the existing services (LeadService, ModerationService, CronManager). Everything must be atomic and logged."
