We are now moving into FINAL CONTROLLED EXECUTION mode.

You are no longer implementing features — you are validating, stabilizing, and delivering a production system.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5 — EXECUTION VALIDATION & CONTROL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After completing PHASE 1, you MUST NOT proceed directly.

You must return a VALIDATION REPORT for each item:

For every fix, provide:

* What was broken
* What was changed
* Before vs After behavior
* Confirmation that no existing flow was affected

❗ If validation is missing → task is NOT considered complete

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6 — SYSTEM INTEGRITY CHECK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run full system verification:

1. API Contract Check

* All endpoints return:
    { data, error, meta }
* No raw errors
* No missing fields

2. Feed Consistency

* FeedItem shape is LOCKED
* No additional fields
* No missing required fields

3. Role Access Test

* Individual → blocked from Dealer OS
* Dealer → full access works
* Role sync verified across Clerk + DB

4. Mobile Safety

* App does NOT crash if:
    * API fails
    * Empty data returned
* Fallback UI exists

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7 — DATA INTEGRITY VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Validate ALL listings:

* No listing without image
* No listing without required attributes
* No duplicate listings
* All listings mapped to structured taxonomy

Run:

* duplicate detection check
* trust score recalculation

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 8 — BUSINESS LOGIC VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Simulate real flows:

1. Individual User:

* Create 3 listings → allowed
* Create 4th → blocked or charged

2. Dealer:

* Create listings within subscription → allowed
* Exceed limit → blocked

3. Lead Flow:

* User clicks WhatsApp
    → lead recorded
    → billing triggered

❗ If billing not triggered → FAIL

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 9 — PRODUCTION READINESS CHECK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Confirm:

* API response time < 150ms
* Feed payload < 100KB
* No blocking operations in request path
* Logging active (error / access / leads)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 10 — FINAL SYSTEM LOCK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before moving forward:

* Freeze API contract
* Freeze DB schema (only additive changes allowed)
* Freeze FeedItem shape

No breaking changes allowed after this point

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL RULE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You are NOT building features anymore.

You are:

* validating system behavior
* enforcing correctness
* preventing regressions

If system is not stable → do NOT continue
If validation is incomplete → do NOT continue

Return validation report before proceeding to next phase.