- [BANCO BFF Architecture](banco-bff-arch.md) — immutable FeedItem contract + layer rules that all future surfaces must respect
- [BANCO installment label match](banco-payment-display.md) — headline monthly amount + payment-type label must come from the SAME offer (best_offer); lowest_monthly vs best_offer diverge
- [BANCO trust_signal verified](banco-trust-signal.md) — client "verified" filters must include "Top Dealer"; use shared isVerifiedSignal(), only "Private Seller" is unverified
- [BANCO Search NLP](banco-search-nlp.md) — installment filter must use EXISTS subquery not post-filter; Arabic category/price normalization details
- [BANCO Taxonomy Normalization](banco-taxonomy.md) — strict taxonomy/enum/location control; pg_trgm dedup; FK ids in all write paths; seed idempotency & lib/db rebuild gotchas
- [BANCO codegen & API contract](banco-codegen-contract.md) — orval JSON.stringify's binary (Blob) bodies → postprocess restores raw; DB status enum may exceed contract enum
- [BANCO API logging & esbuild-pino](banco-api-logging-build.md) — every pino transport must be listed in build.mjs esbuildPluginPino or boot crashes; 3 rotated log channels under LOG_DIR
- [Composite dist staleness](composite-dist-staleness.md) — generated export "missing" in isolated artifact typecheck but present in lib src → stale gitignored composite dist; fix `tsc -b <lib> --force`
- [Expo typedRoutes regen](expo-typed-routes.md) — new expo-router screens fail typecheck until route types regenerate; the generator only runs when the expo dev server boots
- [Orval text/csv body quirk](orval-csv-codegen.md) — orval JSON.stringify's binary text/csv request bodies; codegen self-heals via api-spec postprocess.mjs
- [BANCO Abuse Control](banco-abuse-control.md) — abuse logic centralized in AbuseService+audit_log; EVERY public surface must spread publicVisibilityConditions() or shadow-banned/flagged content leaks
- [Expo testing harness](expo-testing-harness.md) — runTest can't reach the Expo app (wrong host); verify via screenshot tool; restart_workflow fixes EADDRINUSE/port-prompt hangs
- [BANCO listing role asymmetry](banco-listing-role-asymmetry.md) — create needs only auth but "my listings" needs dealer role; never route non-dealers to /listings/mine
- [BANCO orval react hooks](banco-orval-hooks-react.md) — hooks return full {data,error,meta} envelope (unwrap .data?.data); passing any query option requires queryKey (react-query v5)
- [BANCO mobile contact/apply loop](banco-mobile-contact-loop.md) — no chat/notifications backend; contact+apply+business "notifications" all go through trackLead+getDealerLeads; never fake stored chat
- [BANCO signup pending refs](banco-signup-pending-refs.md) — post-signup intent refs (consent/phone/business-route) must clear on EVERY abandonment path or a returning sign-in inherits them
- [BANCO saves sync (mobile)](banco-saves-sync.md) — signed-in saves = optimistic+backend TOGGLE (non-idempotent, guard reconcile); guests can't save (auth-gated)
- [BANCO mobile auth gate](banco-auth-gate.md) — useAuthGate().requireAuth() is the one guest→sign-up funnel; AuthGateProvider above SessionProvider; listing/[id] lock catches ALL guest detail paths
- [BANCO pnpm override precedence](banco-pnpm-overrides.md) — pnpm v11+ IGNORES package.json pnpm.overrides; overrides live ONLY in pnpm-workspace.yaml now; exact pins bypass minimumReleaseAge:1440
- [BANCO icon-font pinning](banco-icon-font-pinning.md) — pin @expo/vector-icons EXACT to SDK baseline or Android shows app-wide .notdef icon boxes; `expo install --check` won't catch it
- [BANCO chat soft-hide + listing-detail state](banco-chat-soft-hide.md) — conversation delete is per-participant soft-hide; new message MUST clear both *_deletedAt; mobile listing/[id] uses local useState not react-query
- [api-server vitest harness](api-server-testing.md) — shared-prod-DB test rules: unique-per-run keys, child-before-user teardown (verify actual FK cascade), isolated buckets for aggregate detectors, runtime plan prices for money paths, poll fire-and-forget writes
- [Dealer/Admin role gate upgrade](banco-role-gate-upgrade.md) — gated self-signup surfaces need an in-app role upgrade (PATCH /me); new users default to individual so a bare gate = permanent lockout; don't spin on isSuccess alone
- [BANCO dealer spend/ROI](banco-dealer-spend-roi.md) — no BFF spend aggregate; derive from wallet txns (boost+lead), label "Spend & Efficiency"; txns cap 50/page → cursor-paginate windowed sums
- [BANCO auth tenant limits](banco-auth-tenant-limits.md) — Clerk tenant supports only email+pw+OTP/Google/Apple; never fake FB/LinkedIn/phone/2FA/OTP-branding — surface real methods honestly
- [BANCO icons are SVG](banco-android-icon-fonts.md) — icons are lucide-react-native SVG (no TTF/glyph-map at runtime) via @/components/icons; permanently fixes Android tofu; never revert to icon fonts; never nest icons in <AppText>
- [BANCO create preview & taxonomy](banco-create-preview.md) — create-listing preview mirrors TWO server formatters (price 2dp BffService vs installment 1dp/plain-K PaymentService) → silent drift risk; taxonomy module must stay RN-pure; raw_materials is UI-only → industrial
- [BANCO Financing CRM](banco-financing-crm.md) — finance requests = finance_request leads + bank_finance payment_options; CRM fields in lead-keyed sidecar (coalesce status 'new'); manage_financing gate; CSV export via plain cookie fetch not a hook
- [BANCO CORS/origin trust](banco-cors-origin-trust.md) — allowlist BANCO-owned origins ONLY (never *.replit.* suffixes); web=same-origin, mobile=bearer/no-Origin; unsafe-method CSRF guard; api-server dev has NO watch → restart to apply
- [BANCO Market rebrand](banco-market-rebrand.md) — dealer-os UI brand is "BANCO Market" but pkg/dir/slug/URLs stay "dealer-os" on purpose; don't "fix" the split or links break
- [BANCO admin Paymob config](banco-payment-admin-config.md) — config is admin-managed DB-first/env-fallback + async webhook seam (fresh HMAC), but settlement/WalletService untouched & secrets write-only/encrypted
- [Project task list cap](project-tasks-list-cap.md) — listProjectTasks() silently returns only first 100; for refs >100 probe getProjectTask({taskRef}) directly
- [Android tab-bar layering](banco-tabbar-android-layering.md) — rectangular tabBarStyle: keep bg/border OFF the elevated bar (opaque box); ROUNDED capsule shadow-host: opaque bg + matching radius + elevation IS required or Android casts no shadow
- [clerk-expo Expo Go crash](clerk-expo-expo-go-native-module.md) — pin @clerk/expo to EXACT 3.3.1 for Expo Go; 3.4.x+ android spec throws requireNativeModule at import → "Cannot find native module 'ClerkExpo'"; "update to latest" reintroduces it
- [Post-merge drizzle push](post-merge-drizzle-push.md) — post-merge must use `push-force`; bare `push` hits an interactive prompt (no TTY) → column silently never applied to main DB → table-wide "column does not exist" crashes
- [BANCO deploy startup log noise](banco-deploy-startup-logs.md) — published deploy prints transient 500 healthchecks, a triple "maintenance jobs registered" (idempotent guard+advisory locks, 1 process), and a pg SSL verify-full warning; all benign
- [BANCO biometric+theme gating](banco-biometric-theme.md) — biometric app-lock must cover UI during async hydration (not just locked===true) or enabled users flash content on cold boot; theme defaults dark synchronously; delete is verify-only two-phase
- [BANCO i18n rendering boundary](banco-i18n-rendering-boundary.md) — Arabic shows ONLY via AppText (localize the leaf component, not just its caller); UI outside LanguageProvider uses getCurrentLang() snapshot
- [BANCO listing image cropper](banco-listing-crop.md) — custom Expo-Go cropper: integer-safe crop rect (floor origin, clamp rounded size), sync-ref confirm latch, override uploads bytes as-is, removePhoto must clear cropQueue/editAsset
- [BANCO business verification](banco-business-verification.md) — under-review is derived (isBusiness && !isVerified); camera scoped to KYC only; keep OS usage strings + PLAY data-safety in lockstep
- [BANCO mobile search map](banco-mobile-map-view.md) — map is WebView+Leaflet/OSM (no native map, no Google key) for Expo Go; markers embedded as JSON, honest centroid clusters, sandboxed iframe on web, near-me/bbox deferred
- [BANCO promote/boost gating](banco-promote-gating.md) — promote gates on active status (boostListing checks ownership only); FeedItem now carries is_active, so profile grid gates Promote on item.is_active && !is_sponsored (stale dist hides the field → tsc -b lib --force)
- [BANCO staff-role & ban guards](banco-staff-role-guards.md) — owner-count invariant is TOCTOU-prone: serialize role writes via advisory xact lock; ban protects Owners only; advisory key registry
- [BANCO upload content-type & size truth](banco-upload-content-type.md) — server ignores sent filename/content_type/size + extensionless UUID keys; PUT Content-Type is the only stored type; size must be checked from stored metadata server-side
- [BANCO admin bootstrap freeze](banco-admin-bootstrap-freeze.md) — ADMIN_EMAILS only mints the FIRST admin then freezes once any admin exists; no in-app grant UI (ban/unban only) → add later admins via DB
- [BANCO mobile permission minimization](banco-mobile-permission-minimization.md) — app never launches camera (library-only); keeping CAMERA off needs expo-image-picker cameraPermission:false too (plugin re-adds it)
- [BANCO domain & contacts](banco-domain-contacts.md) — banco.app→banco.it migration DONE in code + runtime config verified clean (empty email_provider_config row, no env); only external Google Play listing left (user-owned)
- [BANCO engine-strip flicker](banco-engine-strip-flicker.md) — chip row must gate on facets RESOLVED (!loading) not the fail-open set, or it shows-then-collapses; home animated bar must re-measure on category switch
- [React Compiler component order](react-compiler-component-order.md) — helper components MUST be defined before use; compiler rewrites fn decls to const bindings (no hoisting) → runtime ReferenceError tsc/metro never catch
- [Arabic content audit regex](arabic-content-audit-regex.md) — use `rg '\p{Arabic}'` not `[\xD8-\xDB]` (matches Latin Ø-Û, gives false "no Arabic" results); search limit<=50
- [BANCO icon validation gap](banco-icon-validation-gap.md) — unmapped icon name → CircleAlert "dot" + dev warn; test:icons audits it but isn't in CI (only api-server test runs) so broken icons ship silently; wire test:icons into validation
- [Mutation success vs refresh](banco-mutation-vs-refresh.md) — report a write's success from the write call alone; a best-effort post-write refetch failure must NEVER surface as a mutation error (honesty rule)
- [Home has NO search bar](banco-home-no-search-bar.md) — user forcefully banned a home search pill; search lives only on the Search tab; removing it is intended, NOT scope creep
- [Metro asset-swap cache](metro-asset-swap-cache.md) — overwriting a bundled image in place keeps serving old bytes; clear /tmp/metro-cache + /tmp/metro-file-map-* before restart, then warm the real entry.bundle
- [Fullscreen image viewer gestures](banco-fullscreen-viewer.md) — child Pan in a paging ScrollView must be .enabled(zoomed) or it eats swipes; drive zoom via one applyZoom JS cb; remount per-open via key, not a reset effect
- [Listing publish button gate](banco-publish-gate.md) — publish `disabled` = `submitting` ONLY; any media/quota pre-gate silently kills it for buyer requests (photos optional). handleSubmit is the real gate; gate seller-only specs on !isRequest
- [BANCO root landing artifact](banco-landing-minimal.md) — separate `landing` react-vite artifact owns "/" (fixes prod 404) = dark bg + logo + one دخول button → /banco-mobile/; keep it bare, additive-only
- [BANCO EAS native build](banco-eas-native-build.md) — Expo-Go-only app CAN build a native AAB via EAS, but needs android.package/versionCode+eas.json+EAS env vars AND an expo-build-properties packaging exclude (okhttp5/jspecify META-INF dup) or mergeReleaseJavaResource fails
